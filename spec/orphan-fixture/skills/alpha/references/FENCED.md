# Fenced

Named only inside a fenced block in `LINKED.md`. That is an example, not a
link, so this file is unreachable and is one of the two expected detections.
Deliberate — do not link it from anywhere.
