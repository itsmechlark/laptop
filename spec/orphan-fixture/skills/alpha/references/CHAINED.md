# Chained

Reachable in one hop: `SKILL.md` links `LINKED.md`, and `LINKED.md` links this
file. The negative half of the assertion — reporting this one would mean the
check has stopped honoring the hop the rule allows.
